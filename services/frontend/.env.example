# Origin the typed API client sends requests to (e.g. https://api.example.com).
# Leave unset in dev: requests fall back to the current origin and are handled
# by the Vite dev server's /api proxy (see vite.config.ts).
VITE_API_BASE_URL=
